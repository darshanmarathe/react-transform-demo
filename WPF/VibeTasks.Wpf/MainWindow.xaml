<Window x:Class="VibeTasks.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vibe Tasks" Height="720" Width="1180"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Manage Users..." Click="Users_Click" />
                <MenuItem Header="_Import / Export..." Click="ImportExport_Click" />
                <Separator />
                <MenuItem Header="E_xit" Click="Exit_Click" />
            </MenuItem>
        </Menu>

        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock x:Name="StatusText" Text="Ready" />
        </StatusBar>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#F2F4F8" CornerRadius="10" Padding="12" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal">
                    <Button Content="New Task" Width="100" Margin="0,0,8,0" Click="NewTask_Click" />
                    <Button Content="Edit" Width="80" Margin="0,0,8,0" Click="EditTask_Click" />
                    <Button Content="Complete" Width="90" Margin="0,0,8,0" Click="Complete_Click" />
                    <Button Content="Archive" Width="90" Margin="0,0,8,0" Click="Archive_Click" />
                    <Button Content="Restore" Width="90" Margin="0,0,8,0" Click="Restore_Click" />
                    <Button Content="Delete" Width="80" Margin="0,0,20,0" Click="Delete_Click" />
                    <Button Content="Users" Width="90" Margin="0,0,8,0" Click="Users_Click" />
                    <Button Content="Import / Export" Width="130" Click="ImportExport_Click" />
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock Text="Status" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox x:Name="StatusFilter" Width="130" Margin="0,0,20,0" SelectionChanged="Filter_Changed" />
                <TextBlock Text="Priority" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox x:Name="PriorityFilter" Width="130" Margin="0,0,20,0" SelectionChanged="Filter_Changed" />
                <CheckBox x:Name="ArchivedFilter" Content="Show archived" VerticalAlignment="Center" Checked="Filter_Changed" Unchecked="Filter_Changed" />
            </StackPanel>

            <DataGrid x:Name="TasksGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                      SelectionMode="Single" MouseDoubleClick="TasksGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="2*" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" />
                    <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="110" />
                    <DataGridTextColumn Header="Assigned To" Binding="{Binding AssignedTo}" Width="150" />
                    <DataGridTextColumn Header="Due" Binding="{Binding DueDate, StringFormat=d}" Width="120" />
                    <DataGridCheckBoxColumn Header="Recurring" Binding="{Binding IsRecurring}" Width="95" />
                    <DataGridTextColumn Header="Interval" Binding="{Binding RecurrenceInterval}" Width="110" />
                    <DataGridCheckBoxColumn Header="Archived" Binding="{Binding IsArchived}" Width="90" />
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>
